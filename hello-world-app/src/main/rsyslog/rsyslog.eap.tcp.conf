## put this file to /etc/rsyslog.d/*.conf

module(load="imtcp") # needs to be done just once
input(type="imtcp" port="2514")

# log every host in a separate file with date
$template RemoteHost,"/var/log/%HOSTNAME%-%$YEAR%-%$MONTH%-%$DAY%.log"

$RuleSet remote
*.* ?RemoteHost